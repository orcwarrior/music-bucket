'use strict';

describe('Service: mbNotifications', function() {
  // load the service's module
  beforeEach(module('musicBucketApp'));

  // instantiate service
  var mbNotifications;
  beforeEach(inject(function(_mbNotifications_) {
    mbNotifications = _mbNotifications_;
  }));

  it('should do something', function() {
  });
});
